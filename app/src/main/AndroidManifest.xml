<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.equipe01.minhalista">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_iconapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_iconapp_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splash"
            android:label="splash"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".CadastrarProdutos"
            android:label="@string/title_activity_cadastrar_produtos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NovoProduto"
            android:label="@string/title_activity_novo_produto"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
    </application>

</manifest>